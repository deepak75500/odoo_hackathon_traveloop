# Backend environment template
# Copy to backend/.env for reference, or set these variables in your shell before running python app.py.
# backend/app.py reads these values from os.getenv().

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=deepak123mastermind@gmail.com
SMTP_PASS=uofz grer ugyc licw
SMTP_FROM=Traveloop <your-email@gmail.com>
