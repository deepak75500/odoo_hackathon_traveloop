# Frontend environment template
# Copy to frontend/.env when the frontend should call a specific backend origin.

# For local Vite dev with the proxy in vite.config.js, this may be blank:
# VITE_API_URL=

# For deployed frontend or direct backend calls:
VITE_API_URL=http://localhost:8082
